# DefectDetection 项目静态分析报告

> 分析方式：只读静态分析（未执行构建、未启动服务、未修改任何既有文件）
> 分析时间：2026-08-29
> 说明：行号对应分析时的文件内容；凡属推断而非代码确证的结论，均标注 **[推测]**

---

## 一、项目概览

### 1.1 一句话定位

「智检控 - 精密元件智能监测系统」：一个面向职业院校技能大赛的**工业缺陷检测平台**，采用
**Spring Boot 后端 + Python YOLO 推理微服务 + Vue2 管理后台 + uni-app 小程序** 的四段式架构，
后端内嵌一个 **Swing 桌面客户端**（同一进程，通过 `headless(false)` 启动）。

### 1.2 技术栈清单

| 层次 | 技术 | 版本 / 关键依赖 | 入口 |
|---|---|---|---|
| 后端 | Spring Boot | 3.2.2，Java 17，`pom.xml:8,17` | `DefectDetectionApplication.main()` |
| 后端 ORM | MyBatis-Plus | 3.5.4.1（`pom.xml:169-172`） | — |
| 后端 DB | MySQL | `mysql-connector-j` (runtime) + `mysql-connector-java:8.0.28` **双驱动共存**（`pom.xml:41-45,163-167`） | — |
| 桌面端 | Swing + JFreeChart + JavaFX | javafx 20.0.2、jfreechart 1.5.3（`pom.xml:128-162`） | `Software.init()` |
| 推理 | Python + Flask + Ultralytics YOLO | `predict/main.py` | `http://127.0.0.1:8090` |
| 管理后台 | Vue 2.6 + Vue Router 3 + Element-UI 2.15 | Axios、ECharts 5.6（`web/detection/package.json`） | `src/main.js` |
| 小程序 | uni-app (Vue2) + uCharts | `wxdetect` | `pages/index/index` |
| AI 增强 | Ollama 本地 / 阿里云通义千问 | `application.yaml:39-53` | `AIAnalysisService` |
| 辅助工具 | Node.js（chokidar 目录监听） | `test/image-watcher` | `index.js` |

### 1.3 关键端口与地址

| 服务 | 地址 | 来源 |
|---|---|---|
| Spring Boot | `:8081` | `application.yaml:20` |
| 前端 dev server | `:8082`（`/api` 代理到 8081） | `vue.config.js:3-14` |
| Python 推理 | `127.0.0.1:8090` | `config.json:2-7` |
| Ollama | `localhost:11434` | `application.yaml:46` |
| 小程序直连 | `http://192.168.15.222:8081`（**硬编码内网 IP**） | `wxdetect/pages/**` |
| 遗留 SSE demo | `:3001` | `web/detection/src/data/server.js:37` |
| 遗留告警 mock | `http://localhost:3000/warningdata` | `components/warning.vue:512` |

### 1.4 代码规模

| 模块 | 文件数 | 行数 |
|---|---|---|
| Java 后端源码 | ~130 | 9,934 |
| 前端 `src` | 24 个 `.vue` + 4 个 `.js` | 18,179（含 template/style） |
| 小程序业务代码（不含 uni_modules） | ~20 | 2,169（页面） |
| Python | 2 | 106 |
| SQL | 5 | 797 |
| Node 测试工具 | 3 | ~400 |

---

## 二、目录结构

```
DefectDetection/
├── defectDetection/              【后端 + Swing 桌面端，Maven 单体】
│   ├── pom.xml                   依赖清单
│   ├── src/main/java/com/ggbond/defectdetection/
│   │   ├── DefectDetectionApplication.java   启动入口（同时拉起 Swing）
│   │   ├── common/               通用类：Result/BaseContext/SpringContextUtil/Monitor*
│   │   ├── config/               Spring 配置：MVC/CORS/MyBatis/Swagger/元对象填充
│   │   ├── controller/           13 个 REST 控制器
│   │   ├── dto/                  9 个传输对象
│   │   ├── mapper/               14 个 MyBatis-Plus Mapper
│   │   ├── pojo/                 14 个实体
│   │   ├── service/              16 个接口 + 14 个实现
│   │   ├── util/                 ImgUtil / SseUtil / EncryptionUtil / SystemStatusUtil / BeanUtils
│   │   └── software/             ★ Swing 桌面端 + 业务内核（被 Web 复用）
│   │       ├── common/           Software / ConfigProperties / CommonResource / SysStatus / 线程
│   │       ├── data/DataModule   图表数据统计
│   │       ├── image/            ImageModule( REST + 业务 ) / DetectModel(HTTP 调 Python)
│   │       ├── info/             AOP 日志(Log) / 告警(Warn) / 注解与枚举
│   │       ├── sync/SyncModule   硬件同步控制（空实现）
│   │       └── face/             Swing 界面：MainInterface 等 6 类 + 7 个通用组件 + 5 个对话框
│   ├── src/main/resources/
│   │   ├── application.yaml      Spring + AI 配置
│   │   ├── config.json           模型/图表/告警/运行参数
│   │   ├── attributes.yaml       界面文案配置（遗留）
│   │   └── testImg/              约 1000 张测试图（体积负担）
│   ├── uploads/                  运行时上传：images/ camera/ annotated/
│   ├── detectPicture/            检测结果落盘（git 已忽略）
│   ├── .idea/                    IDE 配置 + shelf（含 40+ 张调试截图）
│   └── target/                   Maven 构建产物（git 已忽略）
│
├── predict/                      【Python 推理微服务】
│   ├── main.py                   Flask，/test + /detect
│   ├── video.py                  离线验证脚本（开发机专用路径）
│   ├── best.pt / v10best.pt      两个 YOLO 权重（共 12 MB）
│
├── web/detection/                【Vue2 管理后台】
│   ├── src/main.js router/index.js utils/{request,sseManager}.js
│   ├── src/views/                Home（菜单框架）+ login
│   ├── src/components/           22 个业务组件
│   └── src/data/                 server.js（遗留 SSE demo）+ 静态 JSON
│
├── wxdetect/                     【uni-app 小程序】
│   ├── pages/                    index / detect / result / history / warning / data / mine / login / checklog
│   ├── api/ network/ common/     接口层（detect.js 与页面直连方式混用）
│   ├── components/               uni-* 组件 + jyf-parser + 自研组件
│   └── uni_modules/              第三方组件库（不纳入分析）
│
├── sql/                          建表语句.sql / 1.sql / 测试数据.sql + 两份副本
├── test/                         image-watcher（Node 目录监听）+ camera 测试脚本
├── install-ollama.ps1            Windows 下 Ollama 安装引导
├── ollama-service-setup.sh       **空文件（0 字节）**
├── 本地大模型部署指南.md          Ollama 部署说明
└── Ubuntu服务器部署指南.md        服务器部署说明
```

---

## 三、文件逐一分析

### 3.1 后端 · 启动与配置

#### `DefectDetectionApplication.java`（68 行）
- **定位**：双模启动器。既是 Spring Boot Web 应用，也用 `SpringApplicationBuilder.headless(false)`（`:38`）保留 AWT 能力，从容器取出 `Software` bean（`:40`）并在 EDT 线程 `SwingUtilities.invokeLater(software::init)`（`:44`）拉起 Swing 窗口。
- **对外接口**：`main(String[])`、`@PostConstruct printDataSourceConfig()`（`:52`）。
- **关键逻辑**：`isApplicationStarted` 静态 volatile 标志防重复启动（`:21,31-35`）；`initializationCount` 计数并在 >1 时打印重复初始化告警（`:54-59`）。
- **依赖**：`SpringContextUtil`（静态上下文）、`Software`（GUI 根）。
- **风险**：
  - `:63` **打印数据库明文密码**到 stdout（注释写明"可临时打印，排查后删除"，但仍在代码中）。
  - `isApplicationStarted` 只对同 JVM 内重复调用 `main()` 有效，无法防多实例进程。
  - `headless(false)` 意味着**无图形环境（Linux 服务器 / Docker）启动时必然失败**，与 `Ubuntu服务器部署指南.md` 冲突。

#### `application.yaml`（54 行）
- `:2-6` MySQL `root/password`，库 `defect_detection`，`useSSL=true`（本地 MySQL 通常未开 SSL，**推测会导致连接告警或失败**）。
- `:7-11` 单文件上限 50MB。
- `:14-18` 禁用 devtools 重启与 livereload（注释说明是为防止桌面窗口重复创建）。
- `:19-22` 端口 8081，响应头上限 16KB。
- `:23-28` MyBatis-Plus 逻辑删除字段 `is_deleted`，1 删 0 未删。
- `:39-53` AI 配置：`mode: local`，本地 `qwen2.5:7b`；云端含 **硬编码 API Key `sk-9316...`**（`:52`）。

#### `config.json`（19 行）+ `ConfigProperties.java`（221 行）
- **定位**：业务参数（非 Spring 配置），由 `ConfigProperties` 用 fastjson2 手工加载。
- **数据结构**：`model{id,ip,port,resStoragePath}`、`charts{granularity}`、`warns{...}`、`running{detectInterval}`。
- **关键实现**：`@PostConstruct loadProperties()`（`:177-212`）读 `src/main/resources/config.json`，解析四个子对象后**同时写入静态字段 `properties`**（`:208-211`）——这是全局静态访问点，`DetectModel`、`DetectResDto` 都从这里取配置。
- **风险**：
  - `:49,66` 等 4 个 `saveConfig()` 都用**硬编码相对路径** `src/main/resources/config.json`。打成 jar 后该路径不存在 → 所有"保存配置"功能失效。
  - 4 个 `saveConfig()` 实现完全重复（每个都完整读-改-写整个文件），`saveProperties()`（`:214-219`）连续调用 4 次 → **4 次全文件读写**。
  - `:181-185` 读取失败仅 `printStackTrace()`，`content` 留空 → 后续 `JSONObject.parseObject("")` 抛 NPE，启动崩溃。

#### 其他配置类

| 文件 | 行 | 职责 | 说明 |
|---|---|---|---|
| `config/MvcConfig.java` | 58 | CORS + 静态资源 | `:26-31` 全局放行 `/**`，`allowCredentials(true)`；`:36-43` 资源映射 |
| `config/MybatisConfig.java` | 19 | 分页插件 | 仅注册 `PaginationInnerInterceptor` |
| `config/MyMetaObjecthandler.java` | 51 | 公共字段自动填充 | `:26-34` insert 时填 createTime/updateTime/createUser/updateUser |
| `config/Swagger3Config.java` | 29 | OpenAPI 文档 | 标题仍为"十六进制说"（遗留） |
| `config/SpringBaseConfig.java` | 37 | 占位符 + 上传限制 | `:34-35` **硬编码 5MB**，与 `application.yaml` 的 50MB 冲突 |
| `config/BeanInjectConfig.java` | 11 | 空类 | 无任何 Bean，**死代码** |
| `config/YAMLPropertySourceFactory.java` | 34 | YAML PropertySource 工厂 | 仅被 `ImageModule` import，**实际未使用**（死代码） |
| `config/JacksonObjectMapper.java` vs `common/JacksonObjectMapper.java` | 59/54 | **同名类重复** | 分处两个包，`MvcConfig` 中对应转换器已注释（`:49-58`）→ 两个都是死代码 |

### 3.2 后端 · Controller 层（13 个）

#### `ImageModule.java`（257 行）— ★ 核心
- **定位**：既是 `@Component("ImageModule")`（业务组件），又是 `@RestController @RequestMapping("/detect")`（`:43-46`）——**业务类与控制器耦合在同一类**，是本项目的架构特征。
- **对外接口**：
  - `POST /detect/img`（`:87`）主检测入口
  - `DetectResDto imgDetect(String)`（`:236`）供第三方控制器复用
  - `static DetectResDto getRes()`（`:76`）
  - `boolean saveAsCheckInfo(DetectResDto)`（`:251`）
  - `static boolean ConnectModelTest()`（`:246`）
- **主流程逐段（`:88-198`）**：
  1. `:99` `ImgUtil.convertToJPGBase64(img)` 统一转 JPEG Base64
  2. `:102-111` 调 `detectModel.detectOne()`；**异常被吞掉**并降级为"原图 + 空缺陷"，只 log 不抛
  3. `:114-124` 取当前工单 ID，无工单时**默认 0** 并 warn
  4. `:130-138` 构造 `DetectLog`，路径 = `resStoragePath\workOrderId\随机名`，落盘后入库
  5. `:140-144` 用 `name` 唯一索引**反查刚插入的记录**拿自增 ID（而非 `save()` 回填）
  6. `:148-165` 逐条缺陷：类别不存在则自动建 `DefectionCategory`；`severityService.evaluateDefection()` 打严重等级；入库并计数
  7. `:168-173` 实际数量与预期不符时**回写修正** `detect_log.defections_sum`
  8. `:176-178` `CommonResource.updateWorkOrder()` 推进工单进度
  9. `:181-191` 更新 `DataModule` 图表数据；**GUI 存在时**刷新面板与图表
  10. `:194` 另起线程向所有 SSE 客户端广播
- **条件分支与边界**：`workOrderId` 兜底 0、图片异常降级、缺陷数不一致修正、`realtimeInterface != null` 判空（服务器模式跳过 GUI）——这几处防御是**后补的**，说明项目经历过"桌面端 → Web 化"改造。
- **性能敏感点**：
  - `:137` 每次检测同步落盘图片
  - `:151-157` **每个缺陷两次 DB 往返**（exists + getOne），N 个缺陷 = 2N 次查询
  - `:194` 每次检测 `new Thread(...)`，**无线程池**，高频检测会导致线程爆炸
- **风险**：
  - `:74` `private static DetectResDto res` —— **静态共享可变对象**，并发请求互相覆盖，且 SSE 广播可能推错数据。
  - `:135` 路径拼接用硬编码 `\\`，Linux 下会生成带反斜杠的文件名。
  - `:207` 初始化图片路径 `src/main/resources/assets/init.png` —— 该路径在当前仓库**不存在**（已确认 resources 下只有 yaml/json），必然走 warn 分支。
  - `:202 @PostConstruct init()` 与 `Software.init()` 中 `:95 imageModule.init()` **重复调用**，`detectModel.init()` 被执行两次。

#### `DetectModel.java`（149 行）— Python 服务客户端
- **定位**：封装对 `http://ip:port/detect` 的 HTTP 调用。
- **对外接口**：`void init()`（`:55`）、`static boolean testHttpConnection()`（`:63`）、`DetectResDto detectOne(String)`（`:104`）。
- **关键实现**：
  - 配置来自 `ConfigProperties.properties.getModelConfig()`，写入 **static 字段** `ip/port/id`（`:44-48,56-58`）——非 Spring 注入，靠静态全局。
  - `:117-120` 健康探测节流：`testRes && lastTime+5000 < now` 才重测。但 `testRes` 初值 `false`（`:42`），**永远为假 → 探测分支永不执行**。
  - `:123` `if(true){...}else{...}` —— else 分支（测试模式）**永远不可达**（死代码）。
  - `:125` `HttpUtil.post(targetUrl, parames)` 无超时设置，Python 服务挂起会拖死 Tomcat 线程。
  - `:138` catch 后 `throw e` 向上抛，由 `ImageModule` 降级处理。
- **风险**：`ip/port` 为静态字段，若 `init()` 未先执行则为 `null`/`0`，URL 变成 `http://null:0/detect`。

#### `DetectInfoController.java`（313 行）
- **对外接口**：

| 方法 | 路径 | 行 | 说明 |
|---|---|---|---|
| GET | `/detectInfo/info/history` | 57 | 分页+工单+时间范围查询 |
| POST | `/detectInfo/info/history` | 93 | **与 GET 完全重复的副本**（93-127 与 57-91 逐行相同） |
| GET | `/detectInfo/info/details` | 130 | 详情 + 图片转 Base64 |
| DELETE | `/detectInfo/info/delete` | 156 | 批量删除，`@LogPoint(Delete)` |
| GET | `/detectInfo/charts/load` | 167 | 5 张图表 |
| PUT | `/detectInfo/charts/set` | 223 | 按工单+粒度重算 3 张图 |
| POST | `/detectInfo/ai/analyze` | 268 | AI 缺陷分析 |

- **关键逻辑**：
  - `:73-75` 条件构造器：参数非 null 才拼接条件（`lqw.eq(jobId!=null, ...)`）。
  - `:78,114` `totalPages = count(lqw)` —— **变量名是 totalPages，实际是总记录数**，前端把它当总条数用，属命名误导。
  - `:81,117` `DetectResDto.getDtoFromEntities()` 会把每张图转 Base64；**任一图读取失败则整个方法返回 null**（见 DTO 分析）→ 下面是 `if(detectResDtoList==null)` 兜底（`:84,120`）。
  - `:283-290` 无缺陷时直接返回固定文案，不调 AI。
- **AI 分析流程（`:268-311`）与 ★ 严重缺陷**：
  1. 查检测记录 → 查缺陷列表
  2. `:293` 调 `aiAnalysisService.analyzeDefections(defectionList)`
  3. `:300-304` 遍历 `defectionList`，仅当 `severityLevel != null && repairSuggestion != null` 才 `updateById`
  4. **问题**：AI 成功路径中 `AIAnalysisService.parseAIResponse()`（`:177-207`）只把结果放进 Map，**从不回写 Defection 实体**。因此这两个字段始终为 null → **AI 分析结果永远不落库**，只有 fallback（本地规则）路径才会写入。
  5. `:308-310` `finally {}` 空块，无实际作用。

#### `AnnotationController.java`（575 行）— 数据标注
- **对外接口**：

| 方法 | 路径 | 行 |
|---|---|---|
| POST | `/annotation/upload/camera` | 59 |
| POST | `/annotation/upload/camera/auto` | 125 |
| GET | `/annotation/images/pending` | 183 |
| PUT | `/annotation/images/{imageId}/reject` | 208 |
| DELETE | `/annotation/images/{imageId}` | 248 |
| GET | `/annotation/images/annotated` | 290 |
| POST | `/annotation/data` | 322 |
| GET | `/annotation/data/image/{imageId}` | 505 |
| GET | `/annotation/files/{filename:.+}` | 526 |

- **状态机**：`RawImage.status`：0 待标注 → 2 已标注 → 3 已检测（注释见 `:246,288`）。注意 **1（标注中）在代码中从未被设置**。
- **关键实现**：
  - `:79-82,135-137,482-484` 三处各自生成 `yyyyMMdd_HHmmss_SSS` 时间戳文件名。
  - `:333-334` 判断是否含"裂痕"/"划痕"决定是否生成带框图。
  - `generateAnnotatedImage()`（`:370-499`）：用 `Graphics2D` 在原图上画双描边矩形 + 渐变标签，裂痕红（409-411）、划痕橙（413-414），输出到 `uploads/annotated/`。
  - `:526-574` 文件服务**按目录顺序探测**：annotated → camera → images。
- **边界与异常**：每个接口都有 try/catch 返回 `Result.fail`；`:212` 同时校验 `isDeleted()==1`；`:338` 取第一条标注的 `rawImageId` 更新整图状态（**假设列表同属一张图**）。
- **风险**：
  - ★ **类别不匹配**：`:334,396,409` 判定"裂痕"，但 YOLO 模型输出的是 **开裂/夹杂/点蚀/划痕/斑块/氧化**（`predict/main.py:47`）。标注端用"裂痕"，检测端用"开裂" → 两套类别体系不统一，"裂痕"分支在自动检测链路中**永远不命中**。
  - `:491` `if (fileExtension.toLowerCase().endsWith(".png"))` —— `fileExtension` 本身已是 `.png`，逻辑上可行但写法冗余；`jpeg` 分支（`:477-478`）设了扩展名却没设 format，仍以 `jpg` 写出。
  - **路径穿越**：`:527` `filename` 直接拼进 `uploads/annotated/` + filename，虽 Spring 对 `{filename:.+}` 有基本解码，但未做 `..` 过滤（**潜在目录穿越**）。
  - `:92` `Files.copy(..., REPLACE_EXISTING)` —— 同名时间戳毫秒级冲突概率低但存在。
  - `generateAnnotatedImage` 抛异常时被 `:349-352` 吞掉，标注保存仍返回成功，用户不知图未生成。

#### `LoginController.java`（87 行）
- **对外接口**：`POST /login/in`（`:34`）、`GET /login/out`（`:70`）。
- **认证逻辑**：`:44` 密码 **MD5**（`DigestUtils.md5DigestAsHex`），无盐；`:46` 账号+MD5 密码联合查询；`:50-64` 成功后置 `online=true`、`BaseContext.setCurrentId`、`session.setAttribute("user", manager.getId())`（**存的是 Integer**）、建立 SSE 连接。
- **风险**：
  - 无验证码、无限流、无锁定 → **可暴力破解**。
  - `:71-83` `/login/out` 返回 `void`，无响应体；`:79-82` 用空 `Manager` + 条件 update，会把 `online` 置 false，但**不关闭 SSE 连接**。
  - `:51-53,64` "已在线"互斥登录逻辑被注释掉 → 同一账号可无限并发登录。
  - MD5 存密码 + `application.yaml` 明文 DB 密码，整体口令安全较弱。

#### `UserCentralController.java`（80 行）
- **对外接口**：`PUT /user/saveInfo`（`:30`）、`PUT /user/savePwd`（`:56`）。
- **逻辑**：`:40-43` 先查账号是否重复再更新（**未排除自身 ID**，改自己资料若保留原账号会误判重复）；`:57-77` 改密码校验旧 MD5 后更新。
- **风险**：`:60` `session.getAttribute("user")==null ? 1 : (Integer)...` —— **未登录时默认操作 ID=1 的账号**，属越权后门。

#### `DashboardController.java`（223 行）
- **对外接口**：`GET /dashboard/data`（`:53`）、`GET /dashboard/pictureInfo`（`:90`，SSE）。
- **逻辑**：`/data` 一次返回最新检测结果 + 统计 + 最近 5 条操作日志。
- **SSE 分支（`:90-172`）**：取 session user（**缺省为 1**，`:96-103`）→ 建连接 → 先发 comment 保活（`:137-144`）→ 另起线程 sleep 100ms 后推两条消息（`:147-169`）。
- **私有方法 `getLatestDetectResult()`（`:177-221`）**：查最新 `DetectLog`（`orderByDesc(time) LIMIT 1`）→ 图片转 Base64 → 查缺陷列表；异常时全部降级为空值。
- **风险**：
  - `:147` 每次 SSE 连接 `new Thread(...)`，无池化。
  - `:134` 与 `LoginController:61` 都调 `sseUtil.connect(userId)`，重复连接会先 `complete()` 掉旧连接（见 `SseUtil:27-31`），**多标签页登录会互相踢**。
  - 前端已改用轮询（`sseManager.js`），该 SSE 端点实际**无消费者**。

#### `OperatorController.java`（198 行）— 操作员/密钥管理
- **对外接口**：`GET /sysManage/key/info`（42）、`POST /sysManage/key/add`（64）、`PUT /sysManage/key/batchPwd`（122）、`PUT /sysManage/key/update`（147）、`DELETE /sysManage/key/delete`（186）。
- **关键实现**：
  - `:52-57` 列表返回时 **解密** `loginPwd` 和 `opPwd` 回传前端 → 密码明文出现在 HTTP 响应中。
  - `:71` `lqw.exists("select login_pwd from operator where login_pwd is not null")` —— **子查询与主表无关联**，等价于"是否存在任意非空密码"，语义存疑。
  - `:87-89` 用 `or()` 组合工号/操作密码查重。
  - `:115-117` 无工号时用自增 ID 填充。
- **★ 缺陷（`:122-145` 批量改密）**：
  - `:128-130` 若传入则加密一次 → `loginPwd`
  - `:136` 又对**已加密的密文再加密一次** → 双重加密
  - `:137` `operatorService.update(operator, lqw)` 中 `lqw` 为**空构造器** → **无 WHERE 条件，更新全表**
  - `:139-144` 判断还写反了：`if(!res)` 时返回"修改成功"，`else` 返回"修改失败"

#### `ApiController.java`（156 行）— API Key 管理
- **对外接口**：`GET /sysManage/api/info`（31）、`POST /sysManage/api/add`（54）、`PATCH /sysManage/api/update`（92）、`DELETE /sysManage/api/delete`（116）。
- **⭐ 内部类 `ApiKeyGenerator`（`:133-155`）**：非静态内部类却含 `static` 方法，前缀 `ma-` + 10 位随机字符（`:137-153`），用 `java.util.Random`（**非安全随机**）。
- **关键实现**：
  - `:61-62` 空串转 `-1`（永久有效）；`:67-68` status 默认 1、remark 默认空。
  - `:98-103` **魔法数字**：`validityTimes==0` → +1000；`validityPeriod==0` → +365。语义不明，推测是为了让"0"表示无限。
- **★ 缺陷**：`:75` 生成的 Key **明文入库**；而 `thirdController.checkApi`（`:131`）用 **MD5 摘要**去查库 → 两者永远不匹配，第三方 API 鉴权**必然失败**。

#### `thirdController.java`（145 行）— 第三方开放接口
- **对外接口**：`GET /metalDetect/currentRes`（41）、`POST /metalDetect/detect`（58）、`GET /metalDetect/charts`（77）、`POST /metalDetect/control`（93）。
- **类名小写开头**（`:32`），不符合 Java 命名规范。
- **`checkApi(Api, int level)`（`:130-145`）**：MD5 后查库 → 比较 `permissionLevel < level` → 返回 boolean（true = 通过）。
- **★ 严重逻辑取反**：
  - `:51` `if(res){ 成功 }` —— 正确
  - `:87` `if(checkApi(api,2)){ return Result.fail("api不存在或权限不足"); }` —— **取反**，合法 Key 被拒、非法 Key 放行
  - `:100` `if(checkApi(api,3)){ return Result.fail(...); }` —— **同样取反**
- **控制接口（`:103-127`）**：用 `OpEnum.Start/Pause/Stop.ordinal()` 与 `opId` 比较，依赖**枚举声明顺序**（`Add,Update,Delete,Login,Start,Stop,Create,Pause,Get,Manipulate` → Start=4, Stop=5, Pause=7）。枚举一旦插入新值，协议即失效。
- **风险**：`:43,79,95` 用 `@RequestBody` 接收 GET 请求参数，部分客户端不支持；`:61` 仅判空不校验文件类型 → `ImgUtil.convertToJPGBase64` 内 `isImage()` 会拒绝非图并抛裸 `Exception`（`ImgUtil:81`）。

#### `WarnController.java`（97 行）
- **对外接口**：`GET /detectInfo/warnings/load`（36）、`GET /detectInfo/warnings/info`（73）、`PUT /detectInfo/warnings/set`（86）。
- **注意**：`WarnController` 与 `DetectInfoController` **共用 `/detectInfo` 前缀**，靠子路径区分，不冲突但耦合。
- **逻辑**：`:53-55` 统计近 24h 告警；`:59-65` 按 1/2/3 三级分别计数生成柱状图；`:62-64` 循环中 `lqw.clear()` 复用同一构造器。
- **★ 缺陷**：`:89` `Long managerId = (Long) session.getAttribute("user");` —— `LoginController:60` 存的是 **Integer** → `ClassCastException`。该接口**调用必崩**。

#### 其余控制器

| 文件 | 行 | 接口 | 要点 |
|---|---|---|---|
| `LogController.java` | 62 | `GET /log/info` | `:47-48` 用 `lt/gt`（严格小于/大于），与 `DetectInfoController` 的 `le/ge` 不一致；`:50` 用 `list(pageInfo, lqw)`（重载形式） |
| `CheckController.java` | 58 | `GET /check/info` | `:44` 用 `checkService.list(pg)`（**注意：传了 Page 却调 list，能分页是因 MP 重载**）；`:45-53` 逐条转 Base64，失败置空串 |
| `DefectionController.java` | 39 | `GET /defection/categories` | 返回全部缺陷类别；**前端未调用**（`annotation.vue:533` 硬编码了类别列表） |
| `ExampleController.java` | 14 | `GET /test/example` | 返回 "Hello, World!"，**测试残留** |
| `Web.java` | 34 | 无 | `@Component`，`init()` 空、`run()` 启 `WebInfoSendThread`；**`DefectDetectionApplication:41` 已注释 → 从未被实例化** |

### 3.3 后端 · Service 层

**接口层**：16 个接口，除 `AIAnalysisService`、`DefectionSeverityService`、`AnnotationTaskService` 外，均继承 MyBatis-Plus `IService<T>`，无自定义方法（`WarningsService:13` 额外声明 `getById` 相关）。
- `service/AnnotationTaskService.java`（5 行）：空类，无注解、无方法 → **死代码**。
- `service/DeviceService.java`（7 行）：**唯一未加 `@Service` 的接口**，靠 `DeviceServiceImpl` 的 `@Service` 生效。

**实现层**：14 个 `ServiceImpl<Mapper, Entity>` 实现，全部为标准空实现（15-18 行），仅 `WarningsServiceImpl:18` 有 `saveWarn()`（调用 `WarningsMapper.saveWarn`，但 **Mapper 未配 XML/注解 SQL → 调用会报错**）。

#### `AIAnalysisService.java`（240 行）— AI 增强
- **定位**：调用大模型评估缺陷等级（1-5）与修复建议。
- **对外接口**：`Map<String,Object> analyzeDefections(List<Defection>)`（`:49`）。
- **流程**：
  1. `buildPrompt()`（`:78-107`）：拼接缺陷类型/置信度/尺寸/位置，要求返回指定 JSON Schema
  2. `callAIAPI()`（`:112-172`）：按 `ai.mode` 选本地/云端；云端加 `Bearer`；`temperature=0.7`、`max_tokens=2000`
  3. `parseAIResponse()`（`:177-207`）：剥 ```` ```json ```` 代码块后 fastjson2 解析
  4. 失败 → `fallbackAnalysis()`（`:212-239`）本地规则兜底
- **边界处理**：`:63-70` 捕获所有异常并 fallback；`:130-132` 云端 Key 缺失抛运行时异常；`:183-193` 兼容带/不带 ``` 的两种返回。
- **性能**：`:44` `RestTemplate` 为**裸 new**，无超时、无连接池、无重试 → 模型响应慢会长期占用 Tomcat 线程。
- **★ 缺陷**：`:215` fallback 中 `new DefectionSeverityService()` **手工 new 而非注入**；且如前所述，成功路径不回写实体。

#### `DefectionSeverityService.java`（122 行）— 本地严重度规则
- **对外接口**：`void evaluateDefection(Defection)`（`:12`）——**原地修改入参**。
- **评分算法（`:24-47`）**：
  ```
  baseScore   = score * 3                    // 置信度，0-3
  sizeScore   = min(l * h * 2, 2.0)          // 面积，0-2（封顶）
  totalScore  = (baseScore + sizeScore) * categoryWeight
  等级映射：<1→1  <2→2  <3→3  <4→4  否则 5
  ```
- **类型权重（`:52-62`）**：划痕 0.8、凹陷 1.2、突出 1.1、抛光 0.7、默认 1.0。
- **★ 缺陷**：权重表只覆盖 **凹陷/突出/抛光/划痕**，而模型实际输出 **开裂/夹杂/点蚀/划痕/斑块/氧化** → 6 类中 5 类走默认权重 1.0，且 `generateRepairSuggestion()`（`:113-115`）对未知类型一律返回"未知缺陷类型,建议人工检查"。**AI 增强与检测模型的类别体系完全脱节**。
- **其他风险**：`:25` `defection.getScore()` 若为空 → NPE（自动拆箱）；`:26` `l*h` 若为像素级数值（如 224×212=47488），`min(47488*2, 2.0)` 恒为 2.0 → **尺寸因子恒取满值，失去区分度**。

### 3.4 后端 · 业务内核（`software` 包）

#### `Software.java`（127 行）— 桌面端主类
- **定位**：系统装配器。`@Component`，由 `main()` 手动取出后 `init()`。
- **流程（`:74-117`）**：读粒度 → 查未完成工单(`is_over=0`)建队列 → `CommonResource.init(queue)` → `dataModule.init()` → `imageModule.init()` → 查设备 → 取图表数据 → **GUI 存在则初始化界面**（`:104-108`，否则打印"服务器模式"）→ 置 NORMAL 状态 → `SystemStatusUtil.init()` → `run()`。
- **依赖**：`WorkOrderService`、`DeviceService`、`ImageModule`、`DataModule`、`SysInfoThread`、`Warn`、`MainInterface(可选)`。
- **风险**：`:74 init()` 与 `:95 imageModule.init()` 叠加 `ImageModule` 自己的 `@PostConstruct init()` → 重复初始化（应用中已有 `initializationCount` 告警机制佐证此问题曾发生）。

#### `CommonResource.java`（180 行）— 全局状态中心
- **定位**：静态全局状态仓库（工单队列、系统状态、计数器）。
- **关键字段**：`currentWorkOrder`、`workOrderQueue`(LinkedList)、`status`(SysStatus)、`seq`、`currentNum=-1`、`detectSum=-1`、`totalDetectSum`、`operatorId=1`。
- **关键方法**：
  - `@PostConstruct getService()`（`:63-66`）：把注入的 `WorkOrderService` 转存静态字段
  - `init(Queue)`（`:72-93`）：队列空时弹 Swing 提示框（**在服务器模式下也会执行** `:84-91`，会出现在无头环境）
  - `updateWorkOrder()`（`:94-118`）：进度到顶则完工出队（`is_over=1` 落库）并切下一单，否则 `currentNum++`
- **★ 风险**：全部状态为 **static**，无任何同步措施；`workOrderQueue` 是**非线程安全的 LinkedList**，却被多个 HTTP 请求线程（`ImageModule:176`、thirdController 控制接口）并发读写。

#### `DataModule.java`（349 行）— 图表统计
- **定位**：缺陷率/缺陷分布/API 调用量统计，供前端 ECharts 消费。
- **对外接口**：`init()`、`updateDataMaps(DetectResDto)`、`generateChartsData(Integer)`、`getChartsByWorkOrderId(id, granularity)`、`getDataMaps()`、三个静态 getter。
- **核心算法**：
  - `updateDataMaps()`（`:84-103`）：增量更新。跨工单时重置局部计数；有缺陷则累加；`currentNum % granularity == 0` 时打点。
  - `updateLineAndData()`（`:107-115`）：折线存**累计缺陷率** `totalDefectiveSum/num`，柱状存**区间缺陷率** `partialDefectiveSum/granularity`，保留 2 位小数。
  - `generateChartsData()`（`:136-232`）：全量重算。**注意 `:170-183` 用 `baseIdx + (i-1)*granularity < id < baseIdx + i*granularity` 按 ID 区间分段，且两端都是严格不等 → 边界记录会被漏掉**。
  - `modifyData()`（`:312-321`）：超过 `xNum=5` 个点就从头部删，滑动窗口。
- **★ 缺陷**：
  - `:256` `detectLogService.list(lqw).get(0).getId()` —— **`generateChartsData` 在 `:153` 做了空判断，此处没做** → 该工单无记录时 `IndexOutOfBoundsException`。
  - `:286` `barData = modifyData(lineData);` —— **应为 `modifyData(barData)`**，柱状图数据被折线图覆盖。
  - `:54,57,60` `totalDefectionsNum`、`defectiveRate`、`highestOccurrenceDefect` 为 **static**，被并发请求共享修改。
  - `:85` `res.getWorkOrderId()` 与 `currentWorkOrderId` 用 `Objects.equals` 比较 Integer，逻辑正确但 `currentWorkOrderId` 非 volatile。

#### `software/info/Log.java`（200 行）— AOP 操作日志
- **定位**：`@Aspect`，拦截 `@LogPoint` 注解的方法与 `OpPanel.*Button(..)`，自动写 `sys_log` 并推送 SSE。
- **切点**：`:70-71` 注解切点；`:73-74` 方法名切点（Swing 按钮）。
- **环绕逻辑（`:76-145`）**：
  - `:89-95` `proceed()`，异常时吞掉并替换为 `Result.fail("未知错误")`（**真实异常被隐藏**）
  - `:97-100` 强制转 `Result` 并判断：**只有成功才记日志**（失败操作不入库，审计不完整）
  - `:111-134` 按 `mainRole` 分支：Manager 取 `args[0]` 作 session 取 user；Operator 取 `CommonResource.getOperatorId()`；Api 遍历参数找 `Api` 类型再**按明文 apiKey 查库**（与 `thirdController` 的 MD5 查法冲突）
  - `:132-134` 无匹配 role 抛异常
- **对外接口**：`static writeLog`、`outputLog`、`handlerLog`、`outputMessage`（`:198` 空方法）、`getClassToName()`、`getTypeToName()`。
- **风险**：`:112` 假设 `args[0]` 一定是 `HttpSession`，若被注解方法第一个参数不是 session → `ClassCastException`；`:192` `typeToName.get(...)` 与 `sysLog.getOperator()` 拼接，operator 为 null 会拼出 "null"。

#### `software/info/Warn.java`（156 行）— 告警监控
- **定位**：`@Aspect` + 后台线程，轮询检测两类告警条件。
- **切点**：`:61-69` `@WarnPoint` 的 `@AfterThrowing` —— **方法体为空**（`:67-69`），注解机制未实现。
- **`checkWarningsThread()`（`:81-156`）**：每秒循环，
  - 条件 1（`:90`）：`缺陷率 > defectiveRateWarningLine(1.0)` 且距上次 > `warningInterval*1000` ms
  - 条件 2（`:117`）：`连续工作秒/60 > continuousWorkingMinutesWarningLine(3600)` 且节流通过
  - 命中 → 落库 `warnings` + 更新 GUI + **new Thread 推 SSE** + 弹 Swing 对话框
- **风险**：`:90` 缺陷率阈值 1.0（100%）且 `DataModule.defectiveRate` 是 0-1 小数 → **实际几乎不可能触发**；`:117` 3600 分钟 = 60 小时，同样极难触发；`:110,139` 每次告警新建线程；`warningInterval` 单位为秒（`:90` 乘 1000），而配置注释未说明。

#### 其他 `software` 类

| 文件 | 行 | 职责 | 状态 |
|---|---|---|---|
| `common/SysStatus.java` | 22 | 状态枚举：NORMAL/WORKING/STOPPING/PAUSING/ClOSING | `ClOSING` 拼写异常 |
| `common/SysInfoThread.java` | 40 | 500ms 轮询刷新状态面板 | `:33-35` `do{sleep}while(ClOSING)` → 关闭态下**不刷新界面且空转睡眠** |
| `common/WebInfoSendThread.java` | 45 | 推送仪表盘数据 | **★ `:42` `sseUtil.sendMessageToAll(...)` 返回 Runnable 却未执行** → 若启动将 100% CPU 空转。目前因 `Web` 未被实例化而不触发 |
| `sync/SyncModule.java` | 31 | 硬件同步控制 | `init/syncControl/updateProperties` **全部空实现**，占位 |
| `info/LogPoint.java` | 16 | 方法注解：`OpEnum value() / Class mainRole() / Class target()` | 运行时可见 |
| `info/WarnPoint.java` | 10 | 方法注解：`WarningsEnum value()` | 无实际处理逻辑 |
| `info/OpEnum.java` | 17 | 操作枚举（Add..Manipulate） | `ordinal()` 被 `thirdController` 当协议用 |
| `info/WarningsEnum.java` | 18 | info(1)/warn(2)/error(3) | 含多余无参构造（`:12`） |
| `image/DetectModel.java` | 149 | 见 3.2 | — |
| `face/**`（18 个 Swing 类） | 共约 2,400 行 | `MainInterface`(JFrame 主窗)、`RealtimeInterface`(检测面板+JFreeChart 图表)、`StatusAndDevicesInterface`、`OperationAndOutputInterface`、`TopMenu`、`DeviceLabel`、`GUIAttributes`；`common/` 下 7 个组件；`dialog/` 下 5 个对话框（工单/设备/异常配置/运行配置/历史工单） | 仅桌面模式生效；Web 模式下 `MainInterface` 等 bean 注入为 null |

### 3.5 后端 · 工具类与通用类

| 文件 | 行 | 关键接口 | 要点与风险 |
|---|---|---|---|
| `common/Result.java` | 99 | `success()/fail()` 各 3 个重载 | 码值：SUCCESS=200 / FAIL=201 / WARN=301 / ERROR=302 / LOG=400 / IMAGE=500。**全部为 int**，前端 `person.vue:82` 却比较 `res.code === '200'`（字符串） |
| `common/BaseContext.java` | 29 | `setCurrentId(Long)` / `getCurrentId()` | ThreadLocal，**无 `remove()`** → Tomcat 线程复用导致**用户信息串号**；只在 `LoginController:59` 写入，其他接口未设置 → 元对象填充的 createUser 多为 null |
| `common/SpringContextUtil.java` | 40 | `getApplicationContext()` / `getBean()` | 标准 `ApplicationContextAware` |
| `util/BeanUtils.java` | 36 | 同上的**重复实现** | 与 `SpringContextUtil` 功能完全重复，**死代码** |
| `util/ImgUtil.java` | 111 | `isImage` / `imageToBase64ByPath` / `generateRandomName` / `saveImageToFile` / `convertToJPGBase64` | `:39-42` 用 `byte[file.length()]` 一次性读入内存，50MB 上限下 **OOM 风险**；`:71-75` **输出流未 try-with-resources**，异常时泄漏；`:80-82` 非图片抛**裸 `Exception`**，无消息；`:84-89` `ImageIO.read` 返回 null 时 `:92` 直接 `getWidth()` → NPE |
| `util/SseUtil.java` | 136 | `connect` / `sendMessage` / `sendMessageToAll` / `removeUser` / `listSseConnect` / `isEmpty` | `ConcurrentHashMap<Long,SseEmitter>`；超时 1500s（`:38`）；`:27-31` 重连时先 complete 旧连接；`:35` UUID 手工拼接去横线（可直接 `replace`）；`errorCallBack()`（`:102`）**定义后未使用**（`:42` 注释掉） |
| `util/EncryptionUtil.java` | 56 | `encrypt` / `decrypt` | **★ 硬编码 DES 密钥 `"MyKey123"`**（`:16`）；**★ `:24` `input.concat(fillment)` 结果未赋值**（String 不可变）→ 补位逻辑完全失效；要求输入**必须 6 位**否则返回 null（`:23-28`）；ECB 模式（相同明文→相同密文） |
| `util/SystemStatusUtil.java` | 323 | `init()` / `getContinuousWorkingSeconds()` / `getStatus()` / `formatByte()` | 基于 OSHI 采集 CPU/内存/磁盘/GPU；内部类 `FbMemoryUsage`、`GPU` |
| `common/MonitorServer.java` | 140 | 系统监控 Bean | 与 `SystemStatusUtil` 功能重叠（**推测为早期版本残留**） |
| `common/MonitorInfoModel.java` | 96 | 监控数据模型 | 同上 |
| `common/JacksonObjectMapper.java` | 59 | 自定义 ObjectMapper | 与 `config/` 下同名类重复，均未启用 |

### 3.6 后端 · POJO / DTO / Mapper

**POJO（14 个）**

| 类 | 表名 | 要点 |
|---|---|---|
| `DetectLog` | detect_log | 含 `dataSource`、`rawImageId`（新增字段，标注链路预留） |
| `Defection` | defection | `l/h/x/y/score` + `severityLevel/repairSuggestion/dataSource/annotationId`（后 4 个为 AI 增强新增） |
| `DefectionCategory` | defection_category | `name/count/createTime`，`count` 字段**从未被更新** |
| `RawImage` | raw_image | status：0待标注/1标注中/2已标注/3已检测；`@TableLogic isDeleted` |
| `AnnotationData` | annotation_data | 完整标注框信息 + 审核字段；`@TableLogic` |
| `WorkOrder` | work_order | `currentNum/detectSum/isOver`，**无 `@TableLogic`** 但表里有 `is_deleted` |
| `Manager` | manager | `@TableField("password") pwd`、`phone_number → phone`；告警开关与通知方式 |
| `Operator` | operator | `jobId/loginPwd/opPwd`；**有 `isDeleted` 字段但无 `@TableLogic` 注解** → 逻辑删除不生效 |
| `Api` | api | `apiKey/permissionLevel/validityPeriod/validityTimes/counts`；`@TableLogic` |
| `Device` | device | 含 `static testConnect()`（**恒返回 true**，桩实现）与 `allDeviceOffline()`（**空条件 update 全表**） |
| `Model` | model | 模型注册表，**代码中无任何调用** |
| `Check` | **checkInfo** | ★ `convertTOCheck()` 用 `"kl"/"jz"/"ds"/"hh"/"bk"/"yh"` 拼音缩写匹配类别，与模型的中文类别**完全不匹配** → 6 个分类计数器恒为 0；**`checkInfo` 表在两个 SQL 脚本中都不存在** |
| `SysLog` | sys_log | `setOperatorType()` 有副作用：顺带设置 `mainRole` 中文名 |
| `Warnings` | warnings | level/type/content |
| `AnnotationTask` | — | **空类，无注解无字段** → 死代码 |

**DTO（9 个）**

| 类 | 行 | 要点 |
|---|---|---|
| `DetectResDto` | 65 | 继承 `DetectLog` + `imgBase64` + `defections`。**★ `getDtoFromEntities()`（`:18-36`）中任意一张图读失败就 `return null`** → 整个历史列表因单张坏图全挂；`generateFromFather()`（`:38-46`）**未拷贝 `dataSource`/`rawImageId`** |
| `ChartsDto` | 22 | `name/type/index/source<T>` 泛型 |
| `DashboardInfoDto` | 22 | 运行秒数/缺陷总数/缺陷率/最高发缺陷/最近操作 |
| `WarningsDto` | 39 | 继承 `Manager`；`@JsonProperty("barChart")` 映射 chartsDto |
| `CheckDto` | 17 | checkList + totalPages |
| `SourceDto` | 59 | `generateXYSeries` / `generateRingData` + 内部 `SeriesDto`；**前端未使用**（推测遗留） |
| `AnnotationDto` | 33 | 标注 DTO，**代码中无引用** |
| `AnnotationTaskDto` | 32 | 标注任务 DTO，**代码中无引用** |
| `RawImageDto` | 29 | 原始图 DTO，**代码中无引用** |

**Mapper（14 个）**：全部为 `extends BaseMapper<T>` 的空接口，无自定义 SQL/注解。
- `WarningsMapper:9` 声明 `void saveWarn(String content)` —— **无 `@Select/@Insert` 注解也无 XML**，被调用时必抛 `BindingException`。

### 3.7 Python 推理服务

#### `predict/main.py`（72 行）
- **对外接口**：`GET /test`（`:10`，返回字符串 `"200"`）、`POST /detect`（`:14`）。
- **流程**：
  1. `:17` 从 form 取 `imgurl`
  2. `:23-25` Base64 解码 → `np.frombuffer` → `cv2.imdecode`
  3. `:32` `cv2.cvtColor(image_np2, cv2.COLOR_RGB2BGR)`
  4. `:34` `model(image, conf=0.4)`
  5. `:37-40` `predicts[0].plot()` 画框 → JPEG → Base64
  6. `:42-57` 组装 `l/h/x/y/score/category`
  7. `:62` 返回 `{"imgBase64":..., "defections":[...]}`
- **★ 缺陷**：
  - **`:32` 色彩通道二次转换**。OpenCV 的 `imdecode` 已返回 **BGR**，此处再做 `RGB2BGR` 等于把 BGR 当 RGB 反解 → **R 与 B 通道互换**。检测框位置不受影响，但输入模型的颜色是错的（对依赖颜色的缺陷会影响精度），且回传的标注图**红蓝失真**。
  - **`:47` 类别映射硬编码**：`{0:开裂, 1:夹杂, 2:点蚀, 3:划痕, 4:斑块, 5:氧化}`，与前端标注的"合格/裂痕/划痕"、Java 严重度权重的"凹陷/突出/抛光"**三套体系互不一致**。
  - `:34` `conf=0.4` 硬编码，不可配置。
  - `:70-71` `app.run(port=8090, host='0.0.0.0')` —— Flask **开发服务器**，单线程、无 WSGI 容器，`0.0.0.0` 对外暴露无鉴权接口。
  - `:70` 模型名 `v10best.pt` 硬编码；`best.pt` 未被使用。
  - `model` 定义在 `if __name__ == '__main__'` 块内（`:70`），若用 `flask run` 或 gunicorn 启动则 `model` 未定义 → **`predict()` 必崩**。
  - 无 `requirements.txt`，依赖（flask/ultralytics/cv2/numpy）未声明。
  - `:21,30,35,61` 大量 `print` 调试输出，无日志框架。

#### `predict/video.py`（33 行）
- **定位**：离线模型验证脚本。
- **状态**：主体代码（实时摄像头循环）**全部被注释**（`:7-31`），仅保留 `:32-33` 的 `model.val()`。
- **★ 缺陷**：`:33` 硬编码开发机绝对路径 `D:\paddledetection\PaddleDetection\dataset\my_yolov8/data.yml` —— **在其他机器上无法运行**，属个人调试残留。

### 3.8 数据库脚本

| 文件 | 行 | 内容 | 状态 |
|---|---|---|---|
| `sql/1.sql` | 520 | Navicat 导出（2025-08-12），含 **13 张表** + 数据 | **当前唯一完整 Schema** |
| `sql/建表语句.sql` | 170 | 手写 Schema，**11 张表** | **★ 已过期** |
| `sql/建表语句 - 副本.txt` | 170 | 同上副本 | 同样过期 |
| `sql/测试数据.sql` | 107 | 初始账号/API 等数据 | — |
| `sql/测试数据 - 副本.txt` | 107 | 同上副本 | — |

**`1.sql` 表清单**（`grep "^CREATE TABLE"`）：annotation_data, api, defection, defection_category, detect_log, device, manager, model, operator, raw_image, sys_log, warnings, work_order

**★ 关键缺口**：
1. `建表语句.sql` **缺少** `annotation_data`、`raw_image` 两张表，且 `defection` 表**缺少** `severity_level`、`repair_suggestion`、`data_source`、`annotation_id` 四个字段 → 照此脚本建库，标注功能与 AI 增强**全部报错**。
2. **`checkInfo` 表（Check 实体的 `@TableName`）在两个脚本中都不存在** → `CheckController` 与 `ImageModule.saveAsCheckInfo()` 调用时必报 "Table doesn't exist"。
3. `annotation_data.raw_image_id` 有外键指向 `raw_image(id)`（`1.sql:48`），但 `raw_image` 的建表顺序在 `annotation_data` **之后**（`:24` vs `:367`）→ 直接执行会因外键失败。

### 3.9 管理后台（Vue2）

#### 配置与入口

| 文件 | 要点 |
|---|---|
| `package.json` | Vue 2.6 + Element-UI 2.15 + ant-design-vue 1.7 **两套 UI 库并存**；ECharts + v-charts 并存；含未使用的 `components@^0.1.0`（**可疑依赖**） |
| `vue.config.js` | 端口 8082；`/api` 代理到 8081；`:15-30` 为 SSE 特化响应头（**当前前端已不用 SSE，属遗留**） |
| `.env` | `VUE_APP_BASE_API=http://localhost:8080/detect` |
| `src/main.js` | 挂载 ElementUI、`$request`、`$baseUrl` |
| `src/router/index.js` | 22 条路由；`:6-9` 屏蔽重复点击报错；**:58-75 全局守卫**校验 `localStorage.useradmin` |
| `src/utils/request.js` | Axios 封装；**★ `:6` 用 `process.env.VUE_APP_BASEURL`，而 `.env` 定义的是 `VUE_APP_BASE_API`** → `baseURL` 恒为 `undefined`；`:16` 发 `token` 头但后端**无 token 机制**；`:34` 判断 `res.code==='401'`（字符串）与后端 int 不符 |
| `src/utils/sseManager.js` | **名为 SSE 实为轮询**；**★ `:11` `pollingInterval = 500`（注释写 3000）** → 每 500ms 打一次 `/api/dashboard/data`，该接口内含图片 Base64 编码，**负载极重** |
| `src/data/server.js` | 独立 Express SSE demo（:3001），**与项目无关，死代码** |

#### 视图与组件

| 文件 | 行 | 后端接口 | 说明 |
|---|---|---|---|
| `views/login.vue` | 398 | `POST api/login/in`（`:182`，用裸 axios） | 登录成功后写 `localStorage` |
| `views/Home.vue` | 564 | 无 | 侧边菜单框架；**菜单项 12 个**（`:25-115`），分"国赛2026""标注管理""信息""系统管理"四组 |
| `App.vue` | 456 | 无 | **★ 全局"工业边缘高可用"HUD**：`:83-104` 拦截 axios 测 RTT，按 500/1000ms 阈值切换 warning/danger 横幅；`:198-204` 监听 online/offline。**属视觉演示效果，无真实容灾能力** |
| `components/dashboard.vue` | 691 | 订阅 `sseManager` | **★ `:327-328` 在 `deactivated/activated` 中 `close()` 后 `init()`**，与其他订阅者（charts.vue）争抢同一单例 |
| `components/charts.vue` | 1633 | `GET api/detectInfo/info/history`(488,1230)、`GET .../details`(626,1289)、`GET .../charts/load`(719) | 最大的图表页；含**两个重复的订阅/取消订阅块**（`:315-347` 与 `:1161-1190`） |
| `components/info.vue` | 1215 | `GET .../info/history`(372)、`GET .../details`(433)、`DELETE .../info/delete`(476,506)、**`POST api/detectInfo/ai/analyze`(548)** | 历史检测 + AI 分析入口 |
| `components/annotation.vue` | 1130 | `POST /api/annotation/upload/camera`(232,494)、`GET /images/pending`(283)、`POST /data`(466)、`GET /data/image/{id}`(451)、`DELETE /images/{id}`(302) | **★ `:533` `loadDefectCategories()` 硬编码 `[合格,裂痕,划痕]`，未调用现成的 `/defection/categories`**；`:183,360,423` 深度耦合"裂痕"字面量 |
| `components/biaozhu.vue` | 1295 | `GET /images/annotated`(420)、`GET /data/image/{id}`(376)、`PUT /images/{id}/reject`(491)、`DELETE /annotation/images/{id}`(557)、`DELETE api/detectInfo/info/delete`(528) | 历史标注/已标注列表；`:386,414-416` 同样硬编码类别 |
| `components/picture_detection.vue` | 292 | **`POST /api/detect/img`(154)** | 单图检测页；**路由已注册但 `Home.vue` 菜单无入口** |
| `components/apimanager.vue` | 1002 | `GET api/sysManage/api/info`(423)、`POST .../add`(532,590)、`PATCH .../update`(384)、`DELETE .../delete`(463,626) | API Key 管理；`:532` 与 `:590` **两处重复的 add 调用** |
| `components/pwdmanager.vue` | 817 | `GET api/sysManage/key/info`(468)、`POST .../add`(440)、`PUT .../update`(521)、`PUT .../batchPwd`(388)、`DELETE .../delete`(558,593) | 操作员密钥管理 |
| `components/log.vue` | 406 | `GET api/log/info`(210) | 操作日志 |
| `components/warning.vue` | 791 | **★ `PUT http://localhost:3000/warningdata`(512)** | **指向一个不存在的 mock 服务**，后端 `/detectInfo/warnings/*` 三个接口**前端均未调用** |
| `components/person.vue` | 225 | `$request.put('/user/saveInfo')`(75) | **★ 三重缺陷**：① 走 `$request`（baseURL 为 undefined）→ 请求打到 `:8082/user/saveInfo`，**未走 `/api` 代理 → 必 404**；② **未传 `id` 参数**，后端 `savePropertiesHandler(Integer id,...)` 拿到 null；③ `:82` 比较 `res.code==='200'`（字符串 vs int）；④ 读 `localStorage.useradmin1`，与路由守卫的 `useradmin` **键名不一致** |
| `components/password.vue` | 218 | `$request.put('/user/savePwd')`(104) | 同 ① 的代理问题；`:99` 把新密码赋给 `this.user.pwd` 再整体作为 body，但后端用 `@RequestParam` 接收，body 被忽略 |
| `components/camera.vue` | 212 | 无 | 摄像头拍照组件（`getUserMedia` + canvas），拍照后**不上传**；**路由已注册但菜单无入口，且未被任何组件引用** |
| `components/ValidCode.vue` | 223 | 无 | 验证码组件，**未被引用**（登录页 `:182` 未接验证码） |
| `components/detectInfo.vue` | 64 | 无 | 空表格骨架，**无 `tableData` 数据源，未被引用** |
| `components/HelloWorld.vue` | 32 | 无 | Vue CLI 脚手架残留 |

####「国赛 2.0」新增页面 — ★ 全部为纯前端模拟

| 文件 | 行 | 数据源 |
|---|---|---|
| `components/platform.vue` | 2117 | `Math.random()` 噪声（`:515-531,599`）+ `setInterval` 自增（`:749-753,785-800`） |
| `components/OmniInspection.vue` | 1751 | 硬编码"PLC 寄存器模拟表"（`:634`）+ `:735` 定时器扫描 |
| `components/AgvTwinDashboard.vue` | 655 | 静态 2.5D 拓扑（`:487`），CSS 动画驱动 |
| `components/AgvConsole.vue` | 462 | 摇杆/方向键"简易模拟"（`:31`） |
| `components/QualityArchive.vue` | 476 | "模拟划痕/缺陷区域"（`:101`） |
| `components/ActiveLearning.vue` | 446 | "模拟交互式标注边界框"（`:72`） |
| `components/RecheckCenter.vue` | 570 | 静态列表 |

**结论**：这 7 个页面**未发起任何后端请求**，数据全部由 `Math.random()` / `setInterval` 生成，是**演示用可视化外壳**。若后续需求是"让这些页面接真实数据"，需要新增一整套后端接口。

### 3.10 小程序（uni-app）

| 文件 | 行 | 要点 |
|---|---|---|
| `manifest.json` | — | **★ `"name": "nxdc-milktea"`、`appid: wxbee8247ece46340d`** —— 项目脱胎于**奶茶店点单模板**，名称与 appid 未改 |
| `pages.json` | — | 9 个页面；有 tabBar 配置（index/detect/mine） |
| `main.js` | 19 | 挂载 `$util`、引入 `interceptor.js` |
| `App.vue` | 18 | 仅生命周期日志 |
| `network/base.js` | 11 | `baseURL = 'http://localhost:8081/'`（**:5，开发环境真机无法访问**） |
| `network/http.js` | 32 | `myRequest` 封装；**★ `:12` 使用 `window.localStorage`** —— 小程序环境**无 `window` 对象**，此处必报错 |
| `api/detect.js` | 9 | **★ `url:'metalDetect/detect', method:'GET'`** —— 后端是 **POST**，且该封装**无页面使用** |
| `api/detec-datas.js` | 26 | **硬编码静态图表数据**（划痕 57/夹杂 30/点蚀 20/开裂 18/斑块 8/氧化 8） |
| `api/echarts.js` | — | ECharts 适配层 |
| `common/util.js` | — | `formatTime` 等工具 |
| `common/uqrcode.js` | — | 二维码库 |
| `components/interceptor.js` | — | 路由白名单 + `navigateTo/switchTab` 拦截，检查 `uni.getStorageSync('isLogin')` |

**页面**（均**绕过 `myRequest` 直接用 `uni.request` + 硬编码内网 IP `192.168.15.222:8081`**）：

| 页面 | 行 | 接口调用 |
|---|---|---|
| `pages/login/login.vue` | 183 | `:59-60` `POST http://192.168.15.222:8081/login/in` |
| `pages/index/index.vue` | 349 | 纯导航（`:78-88`） |
| `pages/detect/detect.vue` | 209 | `:81` `POST .../metalDetect/detect`（注释自述"仅为示例，非真实的接口地址"）；`:36,54` 登录跳转；`:76` 结果页传参 |
| `pages/detect/result.vue` | 244 | 接收上一页参数渲染，**无请求** |
| `pages/detect/checklog.vue` | 276 | `:82-83` `GET .../check/info` |
| `pages/history/history.vue` | 233 | `:58-59` `GET .../detectInfo/info/history`；`:86-87` `GET .../detectInfo/info/details` |
| `pages/warning/warning.vue` | 235 | **无请求**（静态/本地数据） |
| `pages/data/data.vue` | 94 | **无请求**（使用 `api/detec-datas.js` 静态数据） |
| `pages/mine/mine.vue` | 346 | 纯导航（`:127-149`） |

**整体判断**：小程序是一条**演示性质的次要链路**——基于奶茶模板改造，接口地址硬编码在页面里，未走统一的 `network/http.js`，且 `window.localStorage` 用法在小程序端不可用。**推测当前不可正常运行**。

### 3.11 测试工具与部署脚本

| 文件 | 行 | 要点 |
|---|---|---|
| `test/image-watcher/index.js` | 355 | chokidar 监听 `./watchDir` 新增图片 → POST 到 `/detect/img` → 成功移 `processed/`、失败移 `failed/`。质量较好：`:21` `processingFiles` 去重集合、`:169` 等待文件写入稳定、`:106-151` 文件名冲突重命名 + 跨盘降级为复制删除、`:310-349` SIGINT/SIGTERM 优雅退出（等最多 10s）。**★ 缺陷**：`:229` 用 `axios.head()` 探测 `/detect/img`，而该接口只接受 POST → **健康检查必然失败**（仅影响启动日志）；`healthCheck()` 定义在 `:226` 但**从未被调用** |
| `test/image-watcher/config.js` | 23 | `uploadUrl: http://localhost:8081/detect/img`；`fileStableDelay: 500`ms |
| `test/test_camera_upload.ps1` | 0 | **空文件** |
| `test/ck.json` | 1 | 内容为 `dajiah;` —— **非 JSON，垃圾文件** |
| `test/camera-test-README.md` | — | 摄像头测试说明 |
| `install-ollama.ps1` | ~200 | 检测 ollama → 未装则打开下载页 → `ollama serve` → 列模型 → 交互选择下载 qwen2.5:7b / llama3.1 等 |
| `ollama-service-setup.sh` | **0** | **完全空文件**（推测是 Linux systemd 配置的占位） |
| `本地大模型部署指南.md` | — | Ollama 安装/模型下载/`application.yaml` 配置切换/验证步骤 |
| `Ubuntu服务器部署指南.md` | 15.6KB | Java17/Maven/MySQL8/Python/Node/Nginx 环境搭建 + 部署步骤。**★ 未处理 `headless(false)` 与 Swing 的冲突** —— 按此文档部署到无图形界面服务器会启动失败 |

---

## 四、模块依赖关系

### 4.1 运行时拓扑

```
┌──────────────────┐   /api 代理(8082→8081)   ┌──────────────────────────────┐
│  Vue2 管理后台    │ ───────────────────────► │                              │
│  :8082           │ ◄──── JSON (REST) ──────  │                              │
└──────────────────┘                           │                              │
                                               │   Spring Boot :8081          │
┌──────────────────┐   HTTP 直连(硬编码IP)     │  ┌────────────────────────┐  │
│  uni-app 小程序   │ ───────────────────────► │  │ Controller 层 (13个)   │  │
│  192.168.15.222  │                           │  └───────────┬────────────┘  │
└──────────────────┘                           │              │               │
                                               │  ┌───────────▼────────────┐  │
┌──────────────────┐  POST /detect/img         │  │ ImageModule(组件+控制器)│  │
│ image-watcher    │ ───────────────────────►  │  └───────────┬────────────┘  │
│ (Node 目录监听)  │                           │              │ HTTP Base64    │
└──────────────────┘                           │  ┌───────────▼────────────┐  │
                                               │  │ DetectModel            │  │
                                               │  └───────────┬────────────┘  │
                                               └──────────────┼───────────────┘
                                                              │ :8090
                                               ┌──────────────▼───────────────┐
                                               │ Flask + YOLO (v10best.pt)    │
                                               │ predict/main.py              │
                                               └──────────────────────────────┘

        ┌──────────────────────────────────────────────────┐
        │ Spring Boot 内部（同一进程）                      │
        │  Software ──► CommonResource(静态全局)            │
        │     ├──► ImageModule ──► DetectModel(HTTP)       │
        │     ├──► DataModule  ──► Mapper ──► MySQL        │
        │     ├──► SysInfoThread (500ms 轮询)              │
        │     ├──► Warn.checkWarningsThread (1s 轮询)      │
        │     └──► MainInterface (Swing, headless 下为 null)│
        │  Log(@Aspect) ──► SysLogService + SseUtil        │
        │  AIAnalysisService ──► Ollama:11434 / 通义千问    │
        └──────────────────────────────────────────────────┘
```

### 4.2 核心数据流：一次图像检测

```
上传方 → POST /detect/img (MultipartFile)
  1. ImgUtil.convertToJPGBase64()        统一转 JPEG + Base64
  2. DetectModel.detectOne()  ──HTTP──►  Flask /detect
       └─ YOLO(conf=0.4) → plot() → 标注图 Base64 + defects[{l,h,x,y,score,category}]
  3. DetectLog 落库 (workOrderId, storagePath, defectionsSum)
       └─ ImgUtil.saveImageToFile() 写 detectPicture/{workOrderId}/{uuid}.jpeg
  4. 逐条 Defection：
       ├─ DefectionCategory 不存在则插入
       ├─ DefectionSeverityService.evaluateDefection()  → severityLevel + repairSuggestion
       └─ Defection 落库
  5. CommonResource.updateWorkOrder()   工单进度 +1 / 完工出队
  6. DataModule.updateDataMaps()        增量更新 line/bar/pie 数据
  7. [GUI] RealtimeInterface.updateImageAndTable() + updateCharts()
  8. new Thread( sseUtil.sendMessageToAll(IMAGE_CODE, res) )
  9. return Result.success(res)
```

### 4.3 关键依赖矩阵（谁被谁依赖，按被依赖次数排序）

| 被依赖方 | 主要依赖方 | 说明 |
|---|---|---|
| `Result` | 全部 13 个 Controller + Log 切面 | 统一响应体 |
| `ConfigProperties.properties`（静态） | `DetectModel`、`DetectResDto`、`Software`、`Warn`、`ImageModule` | 全局配置单点，**所有读取路径均为静态** |
| `CommonResource`（静态） | `ImageModule`、`thirdController`、`DataModule`、`Log`、`SysInfoThread` | 全局状态单点 |
| `SseUtil` | `ImageModule`、`DashboardController`、`LoginController`、`Log`、`Warn`、`WebInfoSendThread` | 推送总线 |
| `DataModule` | `ImageModule`、`DetectInfoController`、`DashboardController`、`thirdController`、`Warn` | 统计数据源 |
| `ImageModule` | `Software`、`thirdController`（业务方法） | 检测内核 |
| `DetectResDto` | `ImageModule`、`DetectInfoController`、`DashboardController`、`Check` | 检测结果载体 |
| `ImgUtil` | `ImageModule`、`CheckController`、`DetectInfoController`、`DashboardController`、`DetectResDto`、`Check` | 图片编解码 |
| `MyMetaObjecthandler` | 所有 insert/update | 依赖 `BaseContext`（ThreadLocal），**多数场景为 null** |

### 4.4 已知悬空依赖（引用了但无实现/无消费方）

| 引用方 | 目标 | 状态 |
|---|---|---|
| `WarningsServiceImpl.saveWarn` | `WarningsMapper.saveWarn` | **无 SQL 实现** |
| `DefectionController./defection/categories` | 前端 | **前端未调用**（硬编码替代） |
| `WarnController` 三个接口 | 前端 | **前端未调用**（`warning.vue` 打向 :3000） |
| `DashboardController./dashboard/pictureInfo`(SSE) | 前端 | **前端已改轮询，无消费者** |
| `Log.outputMessage` | — | 空方法 |
| `SyncModule` 三方法 | — | 空实现 |
| `SourceDto` / `AnnotationDto` / `AnnotationTaskDto` / `RawImageDto` | — | 无引用 |
| `AnnotationTask`(pojo) / `AnnotationTaskService` | — | 空类 |
| `MonitorServer` / `MonitorInfoModel` | — | 与 `SystemStatusUtil` 重叠 |
| `config/BeanInjectConfig` / 两个 `JacksonObjectMapper` / `YAMLPropertySourceFactory` | — | 死代码 |
| `BeanUtils` | — | 与 `SpringContextUtil` 重复 |
| `data/server.js` | — | 独立 demo |
| `components/HelloWorld` / `ValidCode` / `detectInfo` / `camera` | — | 未被引用 |

---

## 五、风险点与待确认项

### 5.1 P0 — 功能实际不可用（确认级）

| # | 问题 | 位置 | 证据 |
|---|---|---|---|
| 1 | **第三方 API 鉴权永远失败** | `ApiController:75` 明文存 Key vs `thirdController:131` MD5 查库 | 两种存储/校验方式不一致 |
| 2 | **`/metalDetect/charts`、`/metalDetect/control` 权限判断取反** | `thirdController:87,100` | `if(checkApi(...)) return fail` 应为 `if(!checkApi(...))` |
| 3 | **告警设置接口必崩** | `WarnController:89` 强转 `Long` vs `LoginController:60` 存 `Integer` | `ClassCastException` |
| 4 | **`checkInfo` 表不存在** | `Check` 实体 `@TableName("checkInfo")`；`sql/1.sql`、`建表语句.sql` 均无此表 | `CheckController`、`saveAsCheckInfo()` 运行时报错 |
| 5 | **AI 分析结果不落库** | `DetectInfoController:300-304` + `AIAnalysisService:177-207` | 成功路径不回写 `severityLevel`，更新条件恒不满足 |
| 6 | **个人中心/改密码请求 404** | `.env:1` 定义 `VUE_APP_BASE_API`；`request.js:6` 读 `VUE_APP_BASEURL` | `baseURL` 恒 undefined，`/user/*` 未走 `/api` 代理 |
| 7 | **批量改密码：双重加密 + 全表更新** | `OperatorController:128-137` | `encrypt(encrypt(x))`；`update(operator, 空lqw)` 无 WHERE |
| 8 | **历史检测列表因单张坏图全挂** | `DetectResDto.getDtoFromEntities()` 中途 `return null` | 任一图读取失败 → 整个列表 null |
| 9 | **缺陷类别三套体系互不一致** | `main.py:47`(开裂/夹杂/点蚀/划痕/斑块/氧化) vs `annotation.vue:533`(合格/裂痕/划痕) vs `DefectionSeverityService:52-62`(凹陷/突出/抛光/划痕) | 仅"划痕"三者共有 |
| 10 | **`getChartsByWorkOrderId` 空结果崩溃** | `DataModule:256` `.get(0)` 无空判断 | 该工单无记录 → `IndexOutOfBoundsException` |

### 5.2 P1 — 正确性与数据一致性

| # | 问题 | 位置 |
|---|---|---|
| 11 | Python 端色彩通道二次转换 | `predict/main.py:32` `imdecode` 已返回 BGR，再做 `RGB2BGR` → R/B 互换 |
| 12 | `barData` 被 `lineData` 覆盖 | `DataModule:286` |
| 13 | `EncryptionUtil.encrypt` 补位失效 | `:24` `input.concat(fillment)` 未赋值；且硬编码 DES 密钥 `:16` |
| 14 | 建表脚本过期 | `建表语句.sql` 缺 2 表 4 字段；`1.sql` 外键顺序颠倒（`:24` 早于 `:367`） |
| 15 | `ImageModule.res` 为静态共享可变对象 | `:74`，并发互相覆盖 |
| 16 | `CommonResource` 全静态 + 非线程安全 `LinkedList` | `:42` 等；被 HTTP 线程并发读写 |
| 17 | `DataModule` 静态计数器非线程安全 | `:54,57,60` |
| 18 | `BaseContext` ThreadLocal 无 `remove()` | `BaseContext:12`；Tomcat 线程复用 → 用户串号 |
| 19 | 每次检测 `new Thread`、每次告警 `new Thread`、每次 SSE 推送 `new Thread` | `ImageModule:194`、`Warn:110,139`、`DashboardController:147` |
| 20 | `RestTemplate` 无超时/连接池 | `AIAnalysisService:44` |
| 21 | `HttpUtil.post` 调用 Python 无超时 | `DetectModel:125` |
| 22 | `ImgUtil` 流未 try-with-resources、整文件读入内存 | `:39-42,71-75` |
| 23 | `person.vue` 未传 `id`、比较字符串 `'200'`、localStorage 键名不一致（`useradmin1` vs `useradmin`） | `:75,82,69` |
| 24 | 操作日志只记成功，失败操作无审计 | `Log:98-100` |
| 25 | 登录无验证码/限流/锁定，MD5 无盐 | `LoginController:34-68` |
| 26 | Operator 列表接口解密密码回传前端 | `OperatorController:52-57` |
| 27 | 文件服务未过滤 `..`，存在路径穿越风险 | `AnnotationController:527` |

### 5.3 P2 — 安全与运维

| # | 问题 | 位置 |
|---|---|---|
| 28 | **阿里云 API Key 硬编码进配置文件并入库** | `application.yaml:52` |
| 29 | **数据库明文密码打印到 stdout** | `DefectDetectionApplication:63` |
| 30 | 数据库账号 `root/password` 弱口令 | `application.yaml:3-4` |
| 31 | CORS 全放行 `/**` 且 `allowCredentials(true)` | `MvcConfig:26-31` |
| 32 | `headless(false)` 导致无图形环境无法启动 | `DefectDetectionApplication:38`；与 `Ubuntu服务器部署指南.md` 冲突 |
| 33 | `ConfigProperties` 写死 `src/main/resources/config.json` 相对路径 | `:49,66,82,118,153,181` → 打 jar 后失效 |
| 34 | 上传大小限制不一致（yaml 50MB vs `SpringBaseConfig` 5MB） | `application.yaml:10-11` vs `SpringBaseConfig:34-35` |
| 35 | Flask 开发服务器 + `0.0.0.0` 无鉴权 | `predict/main.py:71` |
| 36 | Python 无 `requirements.txt` | `predict/` |
| 37 | `video.py` 硬编码开发机绝对路径 | `:33` |
| 38 | 小程序源自奶茶模板，`window.localStorage` 在小程序端不可用 | `manifest.json:2`、`network/http.js:12` |
| 39 | 小程序接口地址硬编码内网 IP | `pages/detect/detect.vue:81` 等 4 处 |
| 40 | 仓库体积膨胀：`testImg/` 约 1000 张图、`.idea/shelf/` 含 40+ 调试截图、`target/`、两份 6MB 权重 | 见目录结构 |
| 41 | 空文件/垃圾文件未清理 | `ollama-service-setup.sh`(0B)、`test/test_camera_upload.ps1`(0B)、`test/ck.json`、`.env` 无效变量 |
| 42 | `sseManager` 轮询间隔 500ms 打全量接口（含图片 Base64） | `sseManager.js:11` |
| 43 | `WebInfoSendThread:42` 返回 Runnable 未执行 → 若启动将 100% CPU 空转 | 当前因 `Web` 未实例化而未触发 |

### 5.4 架构层面观察

1. **桌面端与 Web 端强耦合**：`software` 包既是 Swing 界面，又承载 `ImageModule`/`DataModule`/`CommonResource` 等**业务内核**。Controller 直接依赖这些类，导致 Web 部署必须拖着整个 Swing 栈（JavaFX/JFreeChart 依赖），且 `CommonResource` 的静态状态为桌面单用户设计，搬到 Web 多并发场景后成为隐患。
2. **静态全局状态泛滥**：`ConfigProperties.properties`、`CommonResource.*`、`DataModule` 三个静态计数器、`ImageModule.res`、`DetectModel.ip/port`——至少 5 处静态可变全局量，均为并发风险源。
3. **配置双轨制**：Spring 用 `application.yaml`，业务参数用 `config.json`（手工 fastjson 解析），另有 `attributes.yaml` 遗留。三套配置无统一入口。
4. **"演示层"与"功能层"混杂**：7 个「国赛 2.0」页面 + `App.vue` 的"工业边缘容灾 HUD" 均为视觉模拟，与真实后端能力无连接。评估项目真实能力时需明确区分。
5. **三套缺陷类别体系**：这是贯穿 Python / Java / 前端的**最核心的数据一致性问题**，影响严重度评分、修复建议、抽检分类计数、标注框生成四条链路。

### 5.5 待确认项（需你补充的信息）

1. **当前主用形态**：桌面端（Swing）还是 Web 后台？若只保留 Web，是否计划剥离 `software/face` 与 `headless(false)`？
2. **`sql/建表语句.sql` 与 `sql/1.sql` 哪个是权威 Schema**？`checkInfo` 表是遗漏还是需要补建？
3. **缺陷类别以哪套为准**：模型输出的 6 类（开裂/夹杂/点蚀/划痕/斑块/氧化），还是标注端的人工 3 类（合格/裂痕/划痕）？
4. **「国赛 2.0」7 个页面的定位**：继续作为演示外壳，还是需要接真实数据？后者需要新增哪些后端能力？
5. **小程序是否还需要维护**？当前基于奶茶模板、硬编码内网 IP，若要保留需重构网络层。
6. **`predict/` 的部署形态**：本地开发机直跑 Flask，还是需要容器化 / 换 WSGI？两个权重 `best.pt` 与 `v10best.pt` 用哪个？
7. **AI 分析（`AIAnalysisService`）** 是否为必须保留的功能？若保留，需要修复结果落库与超时控制。
8. **`BaseContext` / 登录态**：当前除登录接口外几乎没有鉴权，是否需要补齐 Token/Session 校验？
9. 是否有既定的**评审/演示时间节点**，用于确定修复优先级？

---

## 六、现状摘要

项目是一个**功能骨架完整、演示效果丰富、但存在若干阻断级缺陷的竞赛型系统**：技术栈覆盖 Spring Boot 3 + MyBatis-Plus、Python YOLO 推理、Vue2 管理后台、uni-app 小程序，并以「Swing 桌面端 + Web 后端同进程」的特殊形态组织，后端约 1 万行 Java、前端约 1.8 万行、小程序约 2 千行。

**可用的主干链路**是「图片上传 → Java 调 Python YOLO 检测 → 缺陷入库 + 严重度评估 → 落盘 + 图表统计 + 历史查询 → 前端展示」，以及「摄像头上传 → 人工标注 → 生成带框图 → 标注数据入库」这两条。

**最突出的问题**集中在四处：其一，**缺陷类别存在三套互不兼容的体系**（Python 6 类 / 前端标注 3 类 / Java 权重 4 类），直接导致严重度评分、修复建议、抽检分类计数、标注框生成四条链路失准；其二，**至少 10 处阻断级 Bug** 使第三方 API 鉴权、告警设置、个人中心、批量改密、AI 结果落库等功能实际不可用；其三，**数据库脚本已过期**（`建表语句.sql` 缺 2 表 4 字段，`checkInfo` 表全库缺失）；其四，**7 个「国赛 2.0」页面与 App 层的容灾 HUD 均为纯前端模拟**，不产生任何后端调用。

**工程健康度方面**，仓库存在大量死代码（12 处悬空引用）、重复实现（2 套 ApplicationContext 工具、2 个同名 JacksonObjectMapper）、空文件与垃圾文件，以及 `testImg/` 千张图片与 `.idea/shelf/` 调试截图造成的体积膨胀；安全性上有硬编码阿里云 Key、明文打印数据库密码、MD5 无盐口令、CORS 全放行等问题。

**建议的优先级**：先做类别体系对齐与 Schema 统一（这是数据正确性的地基），再清 P0 阻断级 Bug，最后按演示需要决定「国赛 2.0」页面是接真实数据还是明确标注为演示。
